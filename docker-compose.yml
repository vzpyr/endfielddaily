services:
  endfielddaily:
    build: .
    image: endfielddaily
    container_name: endfielddaily
    env_file: .env
    tmpfs:
      - /tmp
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    restart: unless-stopped
